# ==============================================
# DEVELOPMENT CONFIGURATION
# ==============================================

# Database
DATABASE_URL=postgresql://user:password@host:5432/database

# Google OAuth (Get from Google Cloud Console)
# Note: You need TWO client IDs - one for server, one for client
GOOGLE_CLIENT_ID=YOUR_SERVER_CLIENT_ID.apps.googleusercontent.com
VITE_GOOGLE_CLIENT_ID=YOUR_CLIENT_WEB_CLIENT_ID.apps.googleusercontent.com

# Session & Authentication
SESSION_SECRET=your-super-secret-session-key-min-32-chars
JWT_SECRET=your-jwt-secret-min-32-chars-for-local-auth
JWT_EXPIRY=7d

# CORS (comma-separated list of allowed origins - hostnames only, no protocols)
ALLOWED_ORIGIN=localhost,127.0.0.1

# Email (Optional - for SendGrid)
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=

# File Upload (Optional)
MAX_FILE_SIZE=10485760
UPLOAD_DIR=./uploads

# AI Services (Optional)
GEMINI_API_KEY=your-google-gemini-api-key-here

# Environment
NODE_ENV=development
PORT=5000

# ==============================================
# PRODUCTION CONFIGURATION (Railway Example)
# ==============================================
# Copy these settings to Railway's environment variables:
#
# NODE_ENV=production
# PORT=5000  (Railway will override this automatically)
# DATABASE_URL=<your-neon-or-railway-postgres-url>
# GOOGLE_CLIENT_ID=<your-server-oauth-client-id>
# VITE_GOOGLE_CLIENT_ID=<your-client-web-oauth-client-id>
# SESSION_SECRET=<generate-strong-32+-char-secret>
# ALLOWED_ORIGIN=your-app.up.railway.app
# SENDGRID_API_KEY=<optional-sendgrid-key>
# SENDGRID_FROM_EMAIL=noreply@yourdomain.com
#
# Google OAuth Setup for Production:
# 1. Go to Google Cloud Console → Credentials
# 2. Add authorized JavaScript origins: https://your-app.up.railway.app
# 3. Add authorized redirect URIs: https://your-app.up.railway.app/auth/google/callback
# 4. Use the same client IDs in Railway environment variables
