import { TransformBlock } from "shared/schema";

export function detectOverwrites(transforms: TransformBlock[]): TransformBlock[] {
    // Logic to warn or fix overwrite collisions
    return transforms;
}
