# ezBuildr Project Rules

- The application is tested on a live production environment (e.g. Railway) using the production database, not in a local dev mode. Code changes need to be committed and pushed to the repository so the CI/CD pipeline can deploy the updates before testing can occur on the live URL.
